CGI - Common Getway Interface - Общий шлюзовый интерфейс.
 Обозночает набор соглашений, которые должны соблюдаться Веб-серверами при 
 выполнение ими различных Веб-приложений. В настоящий момент практически повсеместно
 используется более быстрый и безапосный вариант интерфейса FastCGI.

 http://example.com:80/path/to/image.jpg?parameters
Как нетрудно заметить, может существовать еще строка parameters, следующая после
вопросительного знака. В некоторой степени она аналогична командной строке ОС.
В ней может быть все, что угодно, она может быть любой длины (однако следует учитывать,
что некоторые символы должны быть URL-закодированы, см. ниже). Вот как
раз эта-то строка и передается CGI-сценарию. 

 Переменые окружения: REQUEST_UTI;
 В переменной QUERY_STRING сохраняется значение параметры, в переменой REQUEST_METHOD --
  ключевое слово GET.

 URI - Universal Resource Identifier - Уникальный идентификатор ресурса.

 Загаловки передоваемые с запросом.

 POST отличается от GET тем, что он может передовать данные не только через командную стороку,
 но и в самом конце, после всех заголовков.

 CONTENT_TYPE 
 Данный заголовок идентифицирует тип передаваемых данных. Обычно для этого указывается
значение application/x-www-form-urlencoded, определяющее формат, в котором
все управляющие символы (отличные от алфавитно-цифровых и других отображаемых)
специальным образом кодируются. Это тот самый формат передачи, который
используется методами GET и POST. 

 
 
 HTTP_HOST - В данной переменой загаловка есть значение порта подключения с клиентом.

 SERVER_NAME

 HTTP_USER_AGENT - Переменная окружения, указан браузер и операционная система.

 HTTP_REFERER - Содержит URL страници с которой был осуществлен вход.

 CONTENT_LEGHT -  Заголовок содержит строку, являющуюся десятичным предстовлением длины
 данных в байтах, предоваемых методом POST. При использавании метода GET этого заголовка нет.

 HTTP_COOCKIE

 HTTP_ACCEPT - В этом заголовке браузер посылает типы документов которые он понимает.

 
