AuthType Basic
#Эта строка указывает тип аутентификации, который вы реализуете.
# Этот тип будет реализовывать аутентификацию пароля с использованием файла паролей.
AuthName "Restricted Files"
#Это устанавливает сообщение для диалогового окна проверки подлинности.
# Вы должны сохранить это общее, чтобы неавторизованные
# пользователи не получали никакой информации о том, что защищено.
AuthUserFile /etc/phpmyadmin/.htpasswd
#Здесь задается местоположение файла пароля, который будет
# использоваться для аутентификации. Это должно быть за пределами каталогов,
# которые обслуживаются. Мы скоро создадим этот файл.
Require valid-user
#Указывает, что доступ к этому ресурсу должен быть предоставлен
# только авторизованным пользователям. Это то, что фактически заставляет
# неавторизованных пользователей войти.

